# labguard_contracts

Shared contract package for LabGuard API definitions and future generated clients.

Current contents:

- `openapi/labguard.openapi.yaml`: initial contract placeholder aligned with the backend module layout

Planned use:

- OpenAPI-driven client generation
- DTO and enum sharing across backend tooling and future admin surfaces
- contract testing hooks

# LabGuard API

Fastify + TypeScript control plane for LabGuard.

## Responsibilities

- account access and invitations
- device registration and trust lifecycle
- per-device VPN profile provisioning
- remote security actions
- security events and audit logs

## Run

```bash
pnpm install
pnpm dev
```

Copy `.env.example` to `.env` before starting the service.
